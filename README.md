# AirXonix Lite 3D

Реплика классической аркады AirXonix, реализованная на JavaScript с использованием библиотеки Three.js.

## 🎮 Обзор игры
Игрок управляет вертолетом, цель которого — захватить не менее 80% игрового поля, отсекая пустые участки. Игра усложняется с каждым уровнем за счет увеличения количества и скорости врагов.

---

## 🛠 Технический стек
*   **Движок:** [Three.js](https://threejs.org/) (рендеринг 3D-графики).
*   **Звук:** [Web Audio API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Audio_API) (динамический синтез звука без внешних файлов).
*   **Интерфейс:** HTML5 / CSS3 Overlay.

---

## 🧠 Игровая логика и алгоритмы

### 1. Сеточная система (Grid-based)
Поле представляет собой массив ячеек (по умолчанию 50x50). Каждая ячейка может находиться в одном из трех состояний:
*   `0 (EMPTY)`: Свободная зона, где летают шары.
*   `1 (OCCUPIED)`: Захваченная территория («суша»).
*   `2 (TRAIL)`: Временный след, который оставляет вертолет при выходе в пустую зону.

### 2. Алгоритм захвата (Flood Fill)
Когда вертолет возвращается из пустой зоны на «сушу», срабатывает логика захвата:
1.  **Поиск пустот:** Используется алгоритм заливки (**Flood Fill**). Система обходит все пустые зоны.
2.  **Проверка врагов:** Если в изолированной области нет ни одного врага (шара), эта область помечается как захваченная (`OCCUPIED`).
3.  **Конвертация следа:** Весь текущий след вертолета также превращается в «сушу».

### 3. Физика врагов
*   **Воздушные шары (Balls):** Двигаются по пустым ячейкам. При столкновении с границей захваченной зоны или следом игрока — меняют вектор движения (отражение).
*   **Наземные мины (Mines):** Двигаются исключительно по захваченной территории, создавая опасность даже в «безопасной» зоне.

---

## 🚁 Особенности реализации

### Анимация и Визуал (`animation.js`)
*   **Вертолет:** Модель собрана из 3D-примитивов. Реализована анимация вращения основного и хвостового винтов, а также наклон корпуса при движении.
*   **Оптимизация:** Для отрисовки тысяч блоков поля используется `THREE.InstancedMesh`, что позволяет сохранять высокую производительность (60 FPS).

### Динамический звук (`audio.js`)
Звук полностью синтезируется в реальном времени:
*   **Эффект Доплера:** Высота тона двигателя меняется в зависимости от того, летит ли вертолет к камере или от неё.
*   **Синтез рокота:** Используется комбинация `triangle` и `square` волн с низкочастотной модуляцией (LFO) для имитации звука лопастей.
*   **Интерактивность:** Гул двигателя становится выше и громче при ускорении.

### Статистика (StatsTracker)
Игра собирает метрики в реальном времени:
*   **Уровень риска:** Процент времени, проведенного в опасной зоне со следом.
*   **Средняя реакция:** Время между сменами направления движения.
*   **Дистанция:** Общий путь вертолета в виртуальных метрах.

---

## 🚀 Запуск
Для работы игры требуется локальный веб-сервер (из-за использования ES-модулей):
1.  **Node.js:** `npx serve .`
2.  **Python:** `python -m http.server`
3.  Откройте `http://localhost:3000` в браузере.

---
*Разработано в паре с ИИ (Cursor AI)*

